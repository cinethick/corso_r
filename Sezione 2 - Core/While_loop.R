# per stampare da dentro un loop si usa print
while (F) {
  print("Ciao")
}

#per interrompere uno script basta attivare la console e premere ESC

counter <- 1
while (counter < 12) {
  print(counter)
  counter <- counter +1 
}
